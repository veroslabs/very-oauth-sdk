jdk:
  - openjdk11

install:
  - cd android && ./gradlew clean

build:
  - cd android && ./gradlew :veryoauthsdk:build
  - cd android && ./gradlew :veryoauthsdk:publishToMavenLocal
# JitPack will look for the Android library in the android/veryoauthsdk directory
